

PAR USAR O TAILWIND criando pela comunidade -NATIVEWIND
```

  npm install nativewind
  npm install tailwindcss -D
    npx tailwindcss init
    agr add content: ["./App.{js,jsx,ts,tsx}", "./<custom directory>/**/*.{js,jsx,ts,tsx}"],

  fonts:
    npx expo install @expo-google-fonts/roboto @expo-google-fonts/bai-jamjuree expo-font

  pack para suportar SVG.
    expo install react-native-svg
    npm i -D react-native-svg-transformer
     -> criar arquivo metro.config.js
     
  autenticação
    -> npx expo install expo-auth-session expo-crypto

  limpoar cache, depois de instalar mts coisas;
    npx expo start --clear

  npm i axios

  npx expo install expo-secure-store //o rnativer nao tem cookie, usamos essa lib para salver

  router:  igual o nextrouter
    npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar
 ```